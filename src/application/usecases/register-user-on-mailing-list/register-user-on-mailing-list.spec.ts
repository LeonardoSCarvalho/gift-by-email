describe("Regiter User on mailing list use case", () => {
  it.skip("Should add user with complete data in mailing list", async () => {
    //   const users: UserData[] = []
    //   const userRepository: UserRepository = inMemoryUserRepository()
    //   const registerUserOnMailingList = registerUserOnMailingList(userRepository)
    //   const name = "any name"
    //   const email = "any@email.com"
    //   const response = await registerUserOnMailingList.execute({ name, email })
    //   const user = await userRepository.findByEmail(email)
    //   expect(user.name).toBe(name)
    //
  })
})
